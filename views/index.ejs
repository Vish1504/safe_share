<!DOCTYPE html>
<html lang="en">

    <head>
        <!-- <link rel="stylesheet" type="text/css" href="./style.css"> -->
        <title>Safe Share </title>
        <style>
            body {
                background-color: azure;
                text-align: center;
                font-family: Arial, sans-serif;
            }

            h1 {
                font-size: 3em;
                color: navy;
                margin-top: 2em;
            }

            form {
                background-color: white;
                border: 1px solid gray;
                border-radius: 10px;
                width: 50%;
                padding: 2em;
                margin: 2em auto;
            }

            label {
                font-weight: bold;
                margin-right: 1em;
            }

            input[type="file"],
            input[type="password"] {
                padding: 0.5em;
                font-size: 1em;
                border: 1px solid gray;
                border-radius: 5px;
                width: 80%;
                margin-top: 0.75rem;
                margin-bottom: 2rem;
            }

            button[type="submit"] {
                background-color: navy;
                color: white;
                padding: 0.5em 1em;
                border: none;
                border-radius: 5px;
                cursor: pointer;
                margin-top: 1em;
            }

            button[type="submit"]:hover {
                background-color: white;
                color: navy;
            }

            .file-link {
                margin-top: 1em;
                font-size: 1.2em;
                font-weight: bold;
                color: navy;
            }
        </style>
    </head>

    <body>
        <h1>Safe share</h1>
        <% if(locals.fileLink!=null) {%>
            <div style="margin-bottom: 1rem;"> Your file is at <a href="<%= locals.fileLink %>">
                    <%= locals.fileLink %>
                </a>
            </div>
            <%}%>
                <form method="post" action="/upload" enctype="multipart/form-data">
                    <div>
                        <label for="file">Choose an image to upload:</label>
                        <input type="file" id="file" name="file">
                        <br>
                        <label for="password">Enter password</label>
                        <input type="password" name="password" id="password">
                    </div>
                    <div>
                        <button type="submit">Upload Image</button>
                    </div>
                </form>
                </form>
    </body>

</html>